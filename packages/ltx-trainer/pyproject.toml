[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "ltx-trainer-mlx"
version = "0.15.0"
description = "LTX-2 training toolkit ported to MLX for Apple Silicon"
readme = { text = "MLX training utilities for ltx-2-mlx.", content-type = "text/markdown" }
license = "MIT"
license-files = ["LICENSE", "NOTICE.md"]
requires-python = ">=3.11"
authors = [{ name = "dgrauet" }]
keywords = ["mlx", "video-generation", "diffusion", "apple-silicon", "ltx", "training"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Operating System :: MacOS",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "ltx-core-mlx",
    "ltx-pipelines-mlx",
    "mlx>=0.31.0",
    "pydantic>=2.0.0",
    "safetensors>=0.4.0",
    "numpy>=1.26.0",
    "tqdm>=4.65.0",
    "rich>=13.0.0",
    "pyyaml>=6.0",
    "pillow>=10.4.0",
]

[project.optional-dependencies]
wandb = ["wandb>=0.16.0"]
hub = ["huggingface-hub>=0.26.0"]
all = ["ltx-trainer-mlx[wandb,hub]"]

[tool.hatch.build.targets.wheel]
packages = ["src/ltx_trainer_mlx"]
