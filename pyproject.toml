[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "ltx-2-mlx"
version = "0.15.0"
description = "Unofficial LTX-2.5 inference support for Apple Silicon, built on ltx-2-mlx"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE", "NOTICE.md"]
requires-python = ">=3.11"
authors = [{ name = "dgrauet" }]
keywords = ["mlx", "video-generation", "diffusion", "apple-silicon", "ltx"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Operating System :: MacOS",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "ltx-core-mlx",
    "ltx-pipelines-mlx",
]

[project.optional-dependencies]
trainer = ["ltx-trainer-mlx"]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.7.0",
    "ty>=0.0.0a1",
]
all = ["ltx-2-mlx[trainer,dev]"]

[project.urls]
Homepage = "https://github.com/TommyKammy/ltx-2.5-mlx-inference"
Repository = "https://github.com/TommyKammy/ltx-2.5-mlx-inference"

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
ltx-core-mlx = { workspace = true }
ltx-pipelines-mlx = { workspace = true }
ltx-trainer-mlx = { workspace = true }
mlx-lm = { git = "https://github.com/ml-explore/mlx-lm.git", rev = "254d153fdeb6f150edd4fc5a54f9828638481fa8" }

# The importable code lives in the three workspace packages. The root project
# is deliberately a dependency-only metapackage.
[tool.hatch.build.targets.wheel]
bypass-selection = true

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = [
    "E",     # pycodestyle errors
    "W",     # pycodestyle warnings
    "F",     # pyflakes
    "I",     # isort
    "N",     # pep8-naming
    "UP",    # pyupgrade
    "B",     # flake8-bugbear
    "SIM",   # flake8-simplify
    "TC",    # flake8-type-checking
    "RUF",   # ruff-specific
]
ignore = [
    "E501",   # line too long (handled by formatter)
    "B905",   # zip without strict
    "N806",   # uppercase variable names (standard for tensor dims B, C, H, W, T, F, N)
    "RUF005", # iterable unpacking vs concatenation
    "RUF007", # pairwise vs zip (explicit zip is clearer for sigma pairs)
    "RUF059", # unused unpacked variables (standard for tensor shape destructuring B, C, H, W)
    "SIM108", # ternary operator
    "TCH",    # type-checking imports (not worth the complexity for this project)
    "UP007",  # X | Y union syntax (keep Optional for clarity)
]

[tool.ruff.lint.isort]
known-first-party = ["ltx_core_mlx", "ltx_pipelines_mlx", "ltx_trainer_mlx"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "slow: marks tests that require model weights (deselect with '-m \"not slow\"')",
]

[tool.ty]
# Strict-mode opt-in for PYTHON_QU003 (intendant). See ADR-0003 for rationale.

[tool.ty.src]
# Exclude code paths that don't type-check cleanly under strict mode:
# - parity_keyframe tests import upstream PyTorch reference (no torch in deps; runs in a separate venv).
# - ltx-trainer pulls in HF/transformers/datasets which aren't typed as strict deps.
exclude = [
  "tests/parity_keyframe/**",
  "packages/ltx-trainer/**",
]

[tool.ty.rules]
# MLX's stubs don't model `mx.array` arithmetic precisely, so ty flags every
# tensor op (a + b, a * b, indexing) as unsupported-operator / invalid-
# argument-type / not-subscriptable. These are not real bugs — silencing them
# until MLX ships richer stubs.
unsupported-operator = "ignore"
invalid-argument-type = "ignore"
not-subscriptable = "ignore"
unresolved-attribute = "ignore"
invalid-type-form = "ignore"
invalid-method-override = "ignore"
invalid-return-type = "ignore"
call-non-callable = "ignore"
no-matching-overload = "ignore"
unknown-argument = "ignore"
invalid-assignment = "ignore"
not-iterable = "ignore"

[tool.coverage.run]
source = ["packages"]
branch = true

[tool.coverage.report]
show_missing = true
skip_covered = false
